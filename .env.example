# Local Development Configuration
# For deployment, non-sensitive values are in fly.toml and sensitive values should be set with 'fly secrets set'

# Agent Configuration
AGENT_NAME=agent1  # In deployment: set in fly.toml [env]

# P2P Node Configuration
P2P_NODE_PATH=/app/sdk/p2p-node.js  # In deployment: set in fly.toml [env]
P2P_PORT=8000  # In deployment: set in fly.toml [env]
GRPC_PORT=50051  # In deployment: set in fly.toml [env]

# Sensitive Values (in deployment: set these with 'fly secrets set')
PRIVATE_KEY=your_private_key_here
OPENAI_API_KEY=your_openai_key_here

# Logging Configuration
LOG_TO_CONSOLE=true  # In deployment: set in fly.toml [env]

# Note: Replace all placeholder values with actual values
# DO NOT commit the actual .env file with real keys to the repository 